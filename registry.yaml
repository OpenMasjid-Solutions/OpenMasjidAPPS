# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 Hasan Ismail
# OpenMasjidAPPS registry
# ---------------------------------------------------------------------------
# This is the ONLY hand-edited source of truth for the catalog. Each app lives
# in its OWN public repository; this file just lists them. The build script
# (scripts/build-catalog.mjs) fetches each repo's manifest.yaml +
# docker-compose.yml (+ icon/screenshots) and assembles catalog.json — the file
# OpenMasjidOS actually fetches.
#
# To list an app, open a PR adding an entry below. Each entry:
#
#   - id:     <app-id>               # kebab-case, must equal the app's manifest id
#     repo:   <owner>/<repo>         # the app's own GitHub repository
#     commit: <40-char SHA>          # RECOMMENDED — an immutable commit SHA pin
#     ref:    v1.0.0                 # a git TAG or branch (MUTABLE — can be moved)
#     path:   subdir                 # OPTIONAL — if manifest.yaml isn't at the repo root
#
# Pin to a `commit:` (a full 40-char hex commit SHA) for security. Tags and
# branches are MUTABLE — a compromised or turned-malicious app-repo owner can move
# the ref to backdoored content, and the unattended daily rebuild republishes it to
# the catalog.json every install fetches. A commit SHA cannot be moved, so it is the
# only immutable pin. `commit:` (or an equivalent `sha:`) takes precedence over
# `ref:`; a `ref:` that is itself a 40-char SHA is also treated as immutable. If you
# only set a mutable `ref:`, the build prints a ⚠ warning naming the entry (and, when
# the GitHub API is reachable, the SHA it currently resolves to — copy it here).
#
# Requirements an app repo must meet are in CLAUDE.md and docs/BUILDING_AN_APP.md.
# Complete, working reference apps live under examples/ in this repo — copy one
# into a new repo to start.
# ---------------------------------------------------------------------------

apps:
  - id: display
    repo: OpenMasjid-Solutions/OpenMasjidDisplay
    ref: v0.65.0  # v0.65.0 — Raspberry Pi display nodes: a small Pi plugged into the TV draws the timetable itself and plays cameras straight off the network, so the server does no video work for that screen. Opt-in in Settings; existing RTSP decoder screens are unchanged. commit below is the digest-pin that follows the tag; the app's compose pins the @sha256 image digest.
    commit: a32816bf5e3e3576b4a0bcfb400713b12383e98f
  - id: donations
    repo: OpenMasjid-Solutions/OpenMasjidDonations
    ref: v0.35.0                                 # v0.35.0 — parents can now pay tuition ahead on the donation site, even when nothing is due: a family can settle a term up front or clear the year in one go, and the page offers an amount box instead of telling them there is nothing to see. The balance screen now says which of three things is actually true — what is owed, what has already been paid ahead (“you’re $50.00 in credit, which comes off the next bill”), or nothing due — rather than a bare zero, and with several children each child is listed as owing, in credit, or clear. Overpaying is fine: anything beyond the open months is held as that child’s credit and comes off their next invoice automatically. The smallest card payment is $1, matching the school’s own parent portal, so nobody is stopped by a card decline instead of a friendly message. Needs OpenMasjid Students v0.41.0 or newer for the credit display and paying ahead; older versions keep working exactly as before. Plus everything from v0.34.0: paying a particular month lands on the right child’s bill, and from v0.33.0: tuition takes only the child’s Student ID (e.g. YUS1234) with the child’s name shown back to confirm, no PIN to remember or reissue. Donation and Zakat pages, Gift Aid, receipts and your donation totals are untouched. One-click install; commit below is the immutable pin, and the app compose pins the @sha256 image digest
    commit: f9469fb44117f0c5d539ff4b895af3042faf4f8f
  - id: kiosk
    repo: OpenMasjid-Solutions/OpenMasjidKiosk
    ref: v0.9.33                                 # v0.9.33 — families can pay tuition AHEAD at the kiosk: where a family with nothing outstanding used to reach a dead end, there is now a keypad to pay towards the next bill, and OpenMasjid Students holds it as that child’s credit and takes it off their next invoice automatically. The screen says what the account actually is — “Balance due”, “£50 paid ahead — it comes off the next bill”, or “Nothing due” — with each child’s own figure, and a parent can pay part of a balance or more than it. Nothing under £1/$1 reaches the reader, since a smaller charge costs more in card fees than it collects. The keyboard now behaves like a phone’s: press a key and it lights up and lifts the letter above your finger, and the Student ID keypad stays in capitals so you see the YUS1234 you are typing. Also fixes the PIN pad’s “Unlock” button, which had been splitting across two lines since the writing got bigger. Needs OpenMasjid Students v0.41.0 for paying ahead (older versions keep the previous behaviour), and a tablet app update — the new app is on the kiosk’s own setup page. One-click install; commit below is the immutable pin, and the app compose pins the @sha256 image digest
    commit: fb6ec45b831019064ff30c89d947a336e61e1692   # immutable pin; the app's compose pins the @sha256 image digest
  - id: students
    repo: OpenMasjid-Solutions/OpenMasjidStudents
    ref: v0.41.0                                 # v0.41.0 — families can now pay tuition ahead at the kiosk or on the donation site even when nothing is due, and both screens show any credit already on the account instead of a bare "nothing to pay" (the credit comes off the next invoice automatically). Adding a sibling is now one step on a child's own record: pick the child to bring in and they inherit that household's guardians and emergency contacts. The student list shows Age, worked out from the birthday you already record, in place of the Family column — the household still opens when you click a student. A guardian's relation is now a dropdown (Father, Mother, Relative, Other) rather than free text, and phone numbers format themselves as (555) 123-4567 everywhere, including numbers entered years ago. Your uploaded logo no longer looks squashed in Settings and now appears on the test email exactly as parents will see it. One-click install; commit below is the immutable pin, and the app compose pins the @sha256 image digest
    commit: 5cf1e6e9538d50f7a217a7ba71ffb4ee05dc92c3
  - id: parking-attendant
    repo: SyButter/OpenMasjidParkingAttendant
    ref: v0.2.1                                  # v0.2.1 — one-click install + OpenMasjidOS SSO; live board + incorrect-parking alert cards (with the car's photo) that Display can cycle as announcements; per-plate offender strikes; SQLite on /data
    commit: 2cd04c58cdc25f5714f956ef6bbe517be3d062a5   # immutable pin (the v0.2.1 tag commit)
  # Example (uncomment and adapt once the app repo exists):
  # - id: prayer-times-display
  #   repo: OpenMasjid-Solutions/openmasjid-prayer-times-display
  #   ref: v1.0.0
  # - id: announcements-board
  #   repo: OpenMasjid-Solutions/openmasjid-announcements-board
  #   ref: v1.0.0

# Coming-soon teasers — apps that aren't released yet (no repo/image). They show
# in the App Store with a "Coming soon" badge and can't be installed. Metadata
# only here; when one ships, give it a repo + tag and move it up into `apps:`.
coming_soon: []
